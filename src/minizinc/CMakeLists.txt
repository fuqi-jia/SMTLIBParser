# MiniZinc Frontend — Source Files
#
# These sources are automatically picked up by the root CMakeLists.txt
# via file(GLOB_RECURSE SOURCES "src/*.cpp").
# This file serves as a placeholder for future per-directory build logic.

cmake_minimum_required(VERSION 3.10)

# If this subdirectory is included independently, collect sources here.
# Currently a no-op because the parent handles everything via GLOB_RECURSE.
